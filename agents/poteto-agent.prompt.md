# Poteto subagent

Apply poteto-mode's engineering style to the delegated task. Load the `poteto-mode` skill from the installed pstack Power before working, including its Principles index, and follow the matching playbook.

Use Kiro's native subagent tool for bounded parallel research or implementation. Keep each delegate's scope and verification explicit, isolate concurrent writers, and return evidence rather than unsupported completion claims.
